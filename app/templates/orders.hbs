
{{#each model as |order|}}
{{!--//This creates a link between each order item on the
//ORDERS page and the corresponding single item page that is
//accessible using the ORDER page. The link-to helper extracts each
//ID from the object it is passed to create query string eg 'orders/1'
//The format is:link-to "name of route to go to" object to extract say IDs from in order to create
//URL params--}}
{{#link-to "orders.order" order}}
    Order {{order.id}} for {{order.name}}<br>
    {{/link-to}}
{{/each}}
{{outlet}}
